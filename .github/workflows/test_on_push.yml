name: Bygg och testa

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Sätt upp JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'
          cache: 'maven'

      # Maven kommer via wrappern i repot, så bara en JDK behövs. Testerna startar en
      # riktig Postgres via Testcontainers; dockerdemonen finns redan på runnern.
      - name: Bygg och kör tester
        run: ./mvnw -B --no-transfer-progress verify

      - name: Spara testrapporter
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: testrapporter
          path: 'target/surefire-reports/*'
