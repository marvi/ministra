/**
 * Icke-null är normalfallet. Det som verkligen kan vara null säger det med
 * {@code @Nullable} — se till exempel {@code ServiceDay.name} (D-043).
 */
@NullMarked
package ministra;

import org.jspecify.annotations.NullMarked;
