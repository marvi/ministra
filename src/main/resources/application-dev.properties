# Lokal utveckling. Kör med:
#   ./mvnw spring-boot:run -Dspring-boot.run.profiles=dev
#
# Förutsätter en Postgres 16 på localhost:5432. Starta en med:
#   podman run --rm -d --name ministra-db -p 5433:5432 \
#     -e POSTGRES_DB=ministra -e POSTGRES_USER=ministra -e POSTGRES_PASSWORD=ministra \
#     docker.io/library/postgres:16
#
# Port 5433 med flit: 5432 är ofta upptagen av en annan Postgres på utvecklarmaskinen,
# och då kopplar appen tyst upp sig mot fel databas.

gg.jte.development-mode=true
gg.jte.use-precompiled-templates=false

spring.datasource.url=jdbc:postgresql://localhost:5433/ministra
spring.datasource.username=ministra
spring.datasource.password=ministra

ministra.base-url=http://localhost:8080

# Ingen riktig mejlserver i utveckling. Outboxen fyller på ändå, så raderna går att titta
# på i databasen.
spring.mail.host=localhost
spring.mail.port=1025
